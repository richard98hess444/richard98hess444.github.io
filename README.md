## My Personal Github Website.

## Local verification
To test the html files locally, run a server instead of dragging the file in to the browser directly.
```
# Navigate to the project root folder
python3 -m http.server 8000
```
On the browser, enter `http://localhost:8000/projects/proj_tomato.html`.